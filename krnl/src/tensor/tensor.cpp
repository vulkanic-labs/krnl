#include "tensor/tensor.hpp"

namespace krnl
{

    // Tensor::Tensor(Device device, const std::vector<float> &data)
    // {
    //     // dataSize = data.size() * sizeof(float);
    //     // buffer = Buffer(device.get(), dataSize, WGPUBufferUsage_Storage);
    //     // buffer.write(data.data(), dataSize);
    // }

} // namespace krnl
